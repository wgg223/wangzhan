# 服务器部署指南（含 /api/v1 客户端接口）

> 适用版本：v5.0.0（新增 Flutter 客户端 + `/api/v1` 接口层）
> 服务器：`<your-server-ip>`（root / SSH 22）
> 部署目录：`/var/www/<your-domain>`
> 域名：`https://<your-domain>`
> 进程：PM2（`website-admin`）

## 一、部署什么 / 不部署什么

| 内容 | 是否上传 | 说明 |
| --- | --- | --- |
| `server/`（含新增 `routes/api/`、`config/tokens.js`、`middlewares/api-auth.js`、改动的 `app.js`、`db-schema.js`） | ✅ 上传 | deploy.py 整目录上传，新文件自动覆盖 |
| `views/`、`public/`、`package.json` 等 | ✅ 上传 | 本次未改动，全量上传也无影响 |
| `database.sqlite`（服务器端数据库） | ❌ 不上传 | deploy.py 排除规则已含 `database.sqlite`，**服务器数据库不会被覆盖**；启动时自动创建 `api_tokens` 表（`CREATE TABLE IF NOT EXISTS`，幂等） |
| `.env`、`.session_secret`、`backups/`、`public/uploads/` | ❌ 不上传 | 服务器保留原样 |
| `app/`（Flutter 客户端源码） | ❌ 不上传 | 客户端在本地构建，不需要部署到服务器 |

**结论：本次改动只需上传代码 + 重启 PM2，服务器端数据库零操作、零迁移。**

## 二、部署前准备（本机）

```bash
# 1. 安装部署脚本依赖（仅首次）
pip install paramiko

# 2. 准备好服务器密码（两种方式任选）
#    方式A：运行时交互输入
python deploy.py --upload-only
#    方式B：环境变量传入
set DEPLOY_PASS=你的服务器密码 && python deploy.py --upload-only
```

> ✅ 本机已安装 git（2.55.0）+ paramiko（5.0.0），`--upload-changed` 增量上传可直接使用。
> 已为 `D:\桌面\mi` 添加 git safe.directory 例外；deploy.py 排除规则已加入 `app/`（Flutter 源码不上服务器）与 `.mimocode/`。

## 三、部署步骤（推荐：增量部署）

### 第 1 步：上传变更文件（推荐）

```bash
cd D:\桌面\mi
python deploy.py --upload-changed
```

- 自动检测 git 变更（本会话改动：`server/app.js`、`server/config/db-schema.js` 及新增的 `server/routes/api/*`、`config/tokens.js`、`middlewares/api-auth.js`）
- 上传后**自动重启 PM2** 并显示进程状态
- 若从未部署过或担心遗漏文件，可改用下方的 `--upload-only` 全量上传

### 第 1 步（备选）：全量上传代码

```bash
cd D:\桌面\mi
python deploy.py --upload-only
```

- 会自动上传 `server/`（含新增的 `server/routes/api/*` 全部接口文件）、`views/`、`public/`、`package.json` 等
- 上传带文件大小校验，失败会自动重试
- **注意：此模式不会自动重启服务，需继续执行第 2 步**

### 第 2 步：重启 PM2 使新代码生效

`--upload-only` 不会重启进程，需要手动重启（二选一）：

```bash
# 方式A：直接用部署脚本里的交互模式（最省事）
python deploy.py --interactive
# 菜单里选择重启/修复选项，或直接选「健康检查」触发

# 方式B：SSH 到服务器手动重启
ssh root@8.156.91.188
cd /var/www/dalaowang233.top && pm2 restart ecosystem.config.js && pm2 save
```

> ⚠️ 不要用 `python deploy.py --fix-only` 来触发重启：它只在健康检查**失败**时才重启，旧进程还活着时会跳过。

### 第 3 步：验证

```bash
# 1) 服务器本地健康检查（SSH 内执行）
curl http://127.0.0.1:3000/api/v1/health
# 期望: {"status":"ok","version":"1.0.0"}

# 2) 外网验证（本机执行）
curl https://dalaowang233.top/api/v1/health
# 期望: {"status":"ok",...}

# 3) 原网页功能抽查（本机执行）
curl -I https://dalaowang233.top/          # 200
curl -I https://dalaowang233.top/articles  # 200
curl -I https://dalaowang233.top/admin     # 302（跳登录，正常）

# 4) 查看进程日志确认无报错
python deploy.py --logs   # 或 SSH 后: pm2 logs website-admin --lines 30
```

### 第 4 步：注册 / 登录冒烟（验证 token 鉴权链路）

```bash
# 注册一个测试账号
curl -X POST https://dalaowang233.top/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"deploy_test","password":"test123456","nickname":"部署验证"}'
# 期望: {"success":true,"message":"注册成功"}

# 登录拿到 token
curl -X POST https://dalaowang233.top/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"deploy_test","password":"test123456"}'
# 期望: 返回 {"token":"...","user":{...}}

# 验证通过后，可在管理后台删除该测试账号
```

### 第 5 步：客户端 App 连接

1. 修改 `app/lib/config/app_config.dart`：
   ```dart
   static const String serverBaseUrl = 'https://dalaowang233.top';
   ```
2. 按 `app/README.md` 构建 APK / Windows 程序，登录即用。
3. （可选）管理后台的图片上传、备份等功能走 HTTPS 全部自动兼容，无需额外配置。

## 四、完整部署（首次或大版本）

```bash
python deploy.py
```

完整流程：SSH 连接 → 环境检查 → 安装软件（Node/PM2/Nginx/Certbot）→ **服务器数据自动备份**（`backups/backup_*.tar.gz`）→ 清理旧文件（保留 uploads/logs）→ 上传 → `npm install --production` → PM2 启动 → Nginx 配置 → SSL → 系统优化 → 健康监控。

> ⚠️ 完整部署会**删除远程 `scripts/` 目录**并重建——这是设计行为（脚本目录只放健康监控脚本），不影响本项目功能。

## 五、回滚

```bash
python deploy.py --rollback
```

- 回滚到最近一次完整部署时自动生成的 `backups/backup_*.tar.gz`
- 恢复后自动重启并验证
- 注意：`--upload-only` 模式不生成 tar 备份，如需回滚保障，部署前可先在管理后台「备份管理」里手动创建一个完整备份

## 六、服务器端数据库说明

- 保持 **SQLite**（`/var/www/dalaowang233.top/database.sqlite`），由服务器统一管理，所有客户端（网页 / Android / Windows）共享这一份数据。
- 本次升级**不会改动服务器上任何现有数据**；启动时自动新增一张 `api_tokens` 表（存 App 登录 Token 的 SHA-256 哈希），30 天有效。
- 服务器 `npm install` 时会尝试编译 `better-sqlite3`（Node 20 通常可直接编译，性能更好）；编译失败则自动回退 `sql.js`（纯 JS，功能一致）。
- 日常备份：管理后台 → 备份管理（支持数据库/完整备份）；服务器另有每 5 分钟健康检查 + 每 30 分钟自动修复脚本。

## 七、常见问题

| 问题 | 原因 / 解决 |
| --- | --- |
| 上传后接口还是 404 | 忘记重启 PM2，新代码未生效 → 执行「第 2 步」 |
| `--upload-changed` 显示"未检测到变更" | 仓库无新改动属正常；若确实有改动，检查是否已被 .gitignore / deploy.py 排除规则过滤 |
| `/api/v1/health` 返回 HTML 404 | 上传不完整（`server/routes/api/` 缺失）→ 重新 `--upload-only` 并确认日志无 `MODULE_NOT_FOUND` |
| 图片上传报错 | Nginx `client_max_body_size` 已配 50M（上传单张限 15M），检查 `public/uploads/images` 目录权限（deploy.py 已 chmod 755） |
| App 登录提示"登录已过期" | Token 30 天有效；过期后重新登录即可 |
| 服务器重启后 `api_tokens` 表自动创建 | db-schema.js 每次启动执行 `CREATE TABLE IF NOT EXISTS`，无需手工建表 |

## 八、部署后自检清单

- [ ] `https://dalaowang233.top/api/v1/health` 返回 200 JSON
- [ ] 注册→登录→`/api/v1/auth/me` 带 token 返回用户信息
- [ ] 网页首页 / 文章 / 管理后台均正常（与原版本一致）
- [ ] `pm2 status` 显示 `website-admin` online，无崩溃重启
- [ ] 管理后台「备份管理」可正常创建数据库备份
