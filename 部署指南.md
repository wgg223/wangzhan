# 服务器部署指南

> 适用版本：v5.10.0
> 服务器：`<your-server-ip>`（root / SSH 22）
> 部署目录：`/var/www/<your-domain>`
> 域名：`https://<your-domain>`
> 进程：PM2（`website-admin`）
>
> 本文中的 `<your-server-ip>`、`<your-domain>` 均为占位符，请替换为你的实际值。

## 一、部署什么 / 不部署什么

项目根目录的 `.gitignore` 已配置为**白名单模式**：git 只跟踪项目文件，运行产物（数据库、日志、上传文件、密钥、依赖等）自动排除。下表为部署内容清单，按此表执行即可保证服务器干净。

| 内容 | 是否上传 | 说明 |
| --- | --- | --- |
| `server/` | ✅ 上传 | 运行核心（入口 `server/app.js`） |
| `views/` | ✅ 上传 | EJS 模板 |
| `public/` | ✅ 上传 | 静态资源（`uploads/` 内容除外，见下） |
| `package.json`、`package-lock.json` | ✅ 上传 | 依赖清单 |
| `ecosystem.config.js` | ✅ 上传 | PM2 进程配置 |
| `cdn-config.js` | ✅ 上传 | CDN 分流配置（运行时被引用） |
| `.env.example` | ✅ 上传 | 环境变量模板 |
| `README.md`、`AGENTS.md`、`部署指南.md`、`LICENSE`、`.eslintrc.json`、`.eslintignore` | ✅ 可选 | 文档/工程配置，上传无影响 |
| `database.sqlite`（服务器端数据库） | ❌ 不上传 | **服务器数据库不会被覆盖**；启动时自动建表迁移（`CREATE TABLE IF NOT EXISTS`，幂等） |
| `.env`、`.session_secret` | ❌ 不上传 | 服务器独立配置，部署后另行生成 |
| `backups/`、`logs/` | ❌ 不上传 | 运行产物 |
| `public/uploads/` 内容 | ❌ 不上传 | 用户上传文件；仅目录结构随 `public/` 上传 |
| `node_modules/` | ❌ 不上传 | 服务器 `npm install` 重新安装 |

**结论：部署 = 上传项目文件 + 服务器安装依赖/配置环境 + 启动 PM2，服务器端数据库零操作、零迁移。**

## 二、部署方式（三选一）

### 方式 A：git 仓库（推荐，增量最方便）

项目已配置白名单 `.gitignore`，推送到仓库的只会是项目文件，天然不会夹带杂物。

```bash
# 本机（首次）
cd D:\桌面\wangzhan-注释版
git init
git add -A
git commit -m "init"
git remote add origin <your-repo-url>   # GitHub / Gitee 等
git push -u origin main

# 服务器（首次）
git clone <your-repo-url> /var/www/<your-domain>

# 本机（日常更新）
git add -A && git commit -m "update" && git push

# 服务器（日常更新）
cd /var/www/<your-domain> && git pull
```

### 方式 B：rsync 增量同步（推荐本机有 rsync 时）

Windows 用户可用 Git Bash 或 WSL 提供 rsync。`--delete` 会删除服务器上已不存在的文件（与仓库保持严格一致）：

```bash
rsync -avz --delete \
  --exclude 'node_modules' \
  --exclude 'database.sqlite*' \
  --exclude '.env' \
  --exclude '.session_secret' \
  --exclude 'logs' \
  --exclude 'backups' \
  --exclude 'public/uploads/*' \
  -e ssh ./ root@<your-server-ip>:/var/www/<your-domain>/
```

### 方式 C：打包上传（无 git / rsync 时）

利用 git 只打包已跟踪文件（自动遵守 `.gitignore` 白名单），再用 scp 上传：

```bash
# 本机
git init && git add -A && git commit -m "deploy"   # 若尚未提交
git archive --format=tar.gz -o deploy.tgz HEAD

scp deploy.tgz root@<your-server-ip>:/var/www/<your-domain>/

# 服务器
cd /var/www/<your-domain>
tar -xzf deploy.tgz
rm deploy.tgz
```

## 三、服务器端安装与启动（三种方式共用）

```bash
# 1. 安装 Node.js（>=16，建议 20 LTS）与 PM2
curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && apt install -y nodejs
npm install -g pm2

# 2. 进入部署目录，安装依赖
cd /var/www/<your-domain>
npm install

# 3. 配置环境变量（生产必填 SESSION_SECRET）
cp .env.example .env
# 编辑 .env：
#   NODE_ENV=production
#   SESSION_SECRET=<32位以上随机字符串>
#   PORT=3000
#   TRUST_PROXY=1        # 若使用 Nginx 反向代理
# 生成随机密钥：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# 4. 启动（PM2 自动设置 NODE_ENV=production，首次启动自动持久化 .session_secret）
npm run pm2

# 5. 常用运维
npm run pm2:restart      # 重启
npm run pm2:logs         # 查看日志
npm run health           # 健康检查
pm2 save && pm2 startup  # （可选）开机自启
```

> ⚠️ 生产环境请勿使用 `npm start`（仅用于开发）；必须通过 `npm run pm2` 或 `pm2 start ecosystem.config.js` 启动。

## 四、Nginx 反向代理 + HTTPS（推荐）

```nginx
server {
    listen 80;
    server_name <your-domain>;

    client_max_body_size 50m;   # 附件上传需要，可按需调整

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
```

HTTPS 证书（Certbot 一键申请，自动续期）：

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d <your-domain>
```

> 使用反向代理时，`.env` 中 `TRUST_PROXY=1`；站点 URL 建议在 .env 设置 `SITE_URL=https://<your-domain>`（用于 OAuth 回调、邮件链接）。

## 五、部署后验证

```bash
# 1) 服务器本地健康检查（SSH 内执行）
curl http://127.0.0.1:3000/health

# 2) 外网验证（本机执行）
curl https://<your-domain>/health

# 3) 原网页功能抽查（本机执行）
curl -I https://<your-domain>/           # 200
curl -I https://<your-domain>/articles   # 200
curl -I https://<your-domain>/admin      # 302（跳登录，正常）

# 4) 查看进程日志确认无报错
pm2 logs website-admin --lines 30

# 5) 注册 / 登录冒烟（验证账号链路）
curl -X POST https://<your-domain>/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"deploy_test","password":"test123456","nickname":"部署验证"}'
curl -X POST https://<your-domain>/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"deploy_test","password":"test123456"}'
# 验证通过后，在管理后台删除该测试账号
```

## 六、回滚

- **部署前**：在管理后台「备份管理」创建一个完整备份（数据库 + 上传文件 + 配置 + 代码），作为回滚点
- **代码回滚**：git 方式 `git revert <commit>` / 重新上传旧版本代码 + `npm run pm2:restart`
- **数据回滚**：管理后台「备份管理」→ 恢复备份（需输入管理员密码二次确认）

> ⚠️ 数据库文件（`database.sqlite`）不会被部署流程覆盖，正常部署不影响线上数据。

## 七、服务器端数据库说明

- 保持 **SQLite**（`/var/www/<your-domain>/database.sqlite`），由服务器统一管理，网页端与客户端 API 共享同一份数据
- 部署/升级**不会改动服务器上任何现有数据**；启动时自动执行建表与迁移（幂等）
- `npm install` 时会尝试编译 `better-sqlite3`（原生驱动，性能更好）；编译失败自动回退 `sql.js`（纯 JS，功能一致）
- 日常备份：管理后台 → 备份管理（数据库备份 / 完整备份）；也可在服务器配置 cron 定时备份，例如每日凌晨 3 点备份数据库：
  ```bash
  0 3 * * * cp /var/www/<your-domain>/database.sqlite /var/www/<your-domain>/backups/db-$(date +\%F).sqlite
  ```

## 八、常见问题

| 问题 | 原因 / 解决 |
| --- | --- |
| 上传后接口还是 404 | 忘记重启 PM2，新代码未生效 → `npm run pm2:restart` |
| 启动即退出，提示缺少 SESSION_SECRET | `.env` 未配置或未设置生产变量 → 见「三、服务器端安装与启动」第 3 步 |
| `/health` 返回 HTML 404 | 上传不完整（`server/` 缺失）→ 重新上传并确认 `pm2 logs` 无 `MODULE_NOT_FOUND` |
| 图片/附件上传报错 | Nginx `client_max_body_size` 过小（建议 50m）+ 检查 `public/uploads` 目录写权限 |
| 新文件提交后没推到仓库 | `.gitignore` 为白名单模式，新文件需在「放行清单」追加 `!/<文件名>` |
| 服务器重启后数据库表自动创建 | `db-schema.js` 每次启动执行 `CREATE TABLE IF NOT EXISTS`，无需手工建表 |
| `better-sqlite3` 编译失败 | 自动回退 `sql.js`（WASM），功能一致，不影响使用 |

## 九、部署后自检清单

- [ ] `https://<your-domain>/health` 返回 200 JSON
- [ ] 注册 → 登录 → 携带 Token 访问 `/api/v1/auth/me` 返回用户信息
- [ ] 网页首页 / 文章 / 管理后台均正常（302 跳转登录属正常）
- [ ] `pm2 status` 显示 `website-admin` online，无崩溃重启
- [ ] 管理后台「备份管理」可正常创建数据库备份
