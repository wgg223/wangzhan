# 重复功能修复与代码结构优化计划

## 问题总览

通过全面扫描，发现 **20+ 类后端重复** 和 **12 类前端重复**，总计约 **2,500+ 行**可消除的重复代码。

---

## Phase 1: 后端基础工具函数提取

### 1.1 创建 `server/utils/format.js` — 格式化工具
从以下文件提取 `formatBytes`/`formatSize`/`formatUptime`：
- `server/config/monitor.js:152-176` — `_formatBytes`, `_formatUptime`
- `server/routes/admin/site-stats.js:14-30` — `formatUptime`, `formatBytes`
- `server/routes/admin/attachments.js:65-70` — `formatSize`
- `server/routes/admin/backup.js:357-363` — `formatSize`
- `server/routes/admin/maintenance.js:730-736` — `formatSize`

**操作**：创建 `format.js`，各文件改为 `require` 导入。

### 1.2 创建 `server/utils/fs-helpers.js` — 文件系统工具
从以下文件提取 `getDirSize`/`copyDir`/`removeDir`：
- `server/routes/admin/backup.js:342-394` — `getDirSize`, `copyDir`, `removeDir`
- `server/routes/admin/maintenance.js:713-754` — `getDirSize`, `copyDir`（完全相同）
- `server/routes/admin/system-update.js:17-39` — `copyDirCrossPlatform`, `removeDirCrossPlatform`

**操作**：创建 `fs-helpers.js`，统一跨平台逻辑，各文件改为 `require`。

### 1.3 扩展 `server/utils/settings.js` — 添加 upsert + 缓存失效
当前已有 `getSettings()` 和 `getImageConfigs()`。需要添加：
- `upsertSettings(db, settingsMap)` — 统一 settings upsert 逻辑
- `saveImageShareConfigs(db, body)` — 统一 image_configs 保存逻辑
- 修复缓存 key 不一致 bug（`'settings'` vs `'settings:all'`）

**影响文件**：
- `server/routes/admin/settings-basic.js:12-16, 46-55` → 用 `getSettings` + `upsertSettings`
- `server/routes/admin/settings-smtp.js:12-17, 46-53` → 同上
- `server/routes/admin/settings-agreement.js:11-15, 37-44` → 同上
- `server/routes/admin/settings-popup.js:12-16, 33-61` → 同上
- `server/routes/admin/settings.js:16-20, 38-78` → 同上
- `server/routes/admin/maintenance.js:221-225, 636-643` → 同上
- `server/app.js:132-149` → 删除 `getCachedSettings`，改用 `getSettings`
- `server/config/mailer.js:14-43` → `getSmtpConfig` 改用 `getSettings`
- `server/routes/admin/image-share.js:226-288` → 用 `saveImageShareConfigs`
- `server/routes/image-share.js:834-894` → 用 `saveImageShareConfigs`（100% 复制粘贴）

**缓存 bug 修复**：
- `settings-basic.js:59` `settingsCache.delete('settings')` → `settingsCache.delete('settings:all')`
- `settings-popup.js:66` 同上
- `settings.js:81` 同上

### 1.4 提取默认权限授予函数
当前在 5 个位置重复：
- `server/routes/admin/users.js:62-69, 264-272`
- `server/routes/auth.js:684-692, 846-854`
- `server/config/db-seed.js:224-238`

**操作**：在 `server/config/db-helpers.js` 添加 `grantDefaultPermissions(db, userId, grantedBy)`。

### 1.5 统一日志安全包装
`server/utils/error-handler.js` 已有 `safeLogActivity()` 但从未被使用。30+ 处使用 `try { logActivity(...) } catch` 包装。

**操作**：
- 让 `logActivity` 自身内部处理错误（修改 `server/config/activity.js`）
- 或全局替换为 `safeLogActivity` 导入

**高优先级文件**：`auth.js`（~20处）、`settings-basic.js`、`settings-smtp.js`、`backup.js`、`maintenance.js`

### 1.6 修复 `generateSecureToken` 重复
- `server/config/crypto-secure.js:86-88` — 已有实现
- `server/routes/auth.js:75-77` — 本地重新实现

**操作**：`auth.js` 改为 `require` 导入。

### 1.7 修复 `addImageLog` 重复
- `server/utils/image-utils.js:13-15` — 已有 `addImageLog`
- `server/routes/image-share.js:66-69` — 本地重新定义 `addLog`

**操作**：`image-share.js` 改为从 `image-utils.js` 导入。

### 1.8 统一 `isAdminRole` 使用
已有 `isAdminRole()` 在 `server/middlewares/auth.js:199-201`，但以下文件内联重复：
- `server/routes/image-share.js` — 6+ 处 `user.role !== 'admin' && user.role !== 'super_admin'`
- `server/routes/admin/image-share.js` — 2 处
- `server/routes/admin/messages.js` — 1 处

**操作**：全部改为 `isAdminRole(user)` 调用。

### 1.9 创建 `server/utils/response.js` — 响应工具
- `isAjaxRequest(req)` — 统一 AJAX 检测（9 处重复）
- `renderError(res, statusCode, message, req)` — 统一错误页面渲染（20+ 处重复）

**影响文件**：`frontend.js`、`admin/articles.js`、`admin/pages.js`、`admin/novels.js`、`admin/projects.js`、`middlewares/auth.js`、`middlewares/security.js`

### 1.10 修复 `initScheduledBackup` 重复定义 bug
`server/routes/admin/maintenance.js` 中 `initScheduledBackup` 被定义了两次（行 20-42 和 45-67），第一个有 bug（`cronExpr.settingValue` 应为 `cronExpr.setting_value`），第二个覆盖了第一个。

**操作**：删除第一个定义（20-42），保留第二个。

### 1.11 统一 Multer 上传配置
- `server/app.js:118-130` — 未使用的 storage 配置 → 删除
- `server/routes/admin/upload.js:58-71, 100-135` — 通用 + 图片上传
- `server/routes/image-share.js:13-56` — 重复的图片上传配置

**操作**：`image-share.js` 从 `admin/upload.js` 导入，删除 `app.js` 中未使用的配置。

### 1.12 移除重复的 `/home` 路由
`server/routes/frontend.js:46-61` — `GET /home` 与 `GET /` 完全相同。

**操作**：`GET /home` 改为 `res.redirect(301, '/')`。

### 1.13 移除 image-share.js 中的旧管理路由重定向
`server/routes/image-share.js:785-832` — 10 个 admin 重定向路由，指向 `/admin/image-share/*`。

**操作**：删除这些路由（如果仍有外部链接，保留 301 重定向）。

### 1.14 统一项目加载逻辑
3 个位置重复 "从 DB 加载项目，回退到常量" 的模式：
- `server/routes/admin/projects.js:46-56, 83-97`
- `server/routes/admin/reset.js:361-378`（`getAllProjectDefinitions`）

**操作**：`projects.js` 使用 `reset.js` 的 `getAllProjectDefinitions()`，或提取到 `server/utils/project-utils.js`。

### 1.15 提取 reset.js 统计收集函数
`server/routes/admin/reset.js:121-165` 和 `260-309` — 两处完全相同的统计收集代码。

**操作**：提取为 `collectProjectStats(db)` 函数。

---

## Phase 2: 前端工具函数提取

### 2.1 创建 `public/js/utils.js` — 共享前端工具
提取到一个共享文件，在所有 layout 中引入：

- `escapeHtml(text)` — 13 处重复
- `getCsrfToken()` — 27 处重复
- `showToast(message, type)` — 2-3 处重复（admin.js + chat.js）
- `copyToClipboard(text, successMsg)` — 6 处重复
- `formatTime(timestamp)` — 3 处重复
- `csrfFetch(url, options)` — 封装 CSRF token 注入的 fetch（20+ 处重复模式）
- `withButtonLoading(btn, text, asyncFn)` — 按钮加载状态封装（30+ 处重复）
- `openModal(id)` / `closeModal(id)` — 5 处重复
- `updateBadge(endpoint, elementId)` — 参数化的 badge 更新

### 2.2 提取 `public/js/media-selector.js` — 媒体选择器
从 3 个编辑器模板提取 ~180 行重复代码：
- `views/frontend/article-editor.ejs:193-377`
- `views/admin/article-editor.ejs:385-570`
- `views/admin/page-editor.ejs:185-346`
- `public/js/admin.js:124-186`

**操作**：创建 `media-selector.js`，各模板改为引入该文件。

### 2.3 提取 `public/js/attachment-upload.js` — 附件上传
从 2 个文章编辑器提取 ~300 行重复代码：
- `views/frontend/article-editor.ejs:380-700+`
- `views/admin/article-editor.ejs:572-890+`

**操作**：创建 `attachment-upload.js`，各模板改为引入该文件。

### 2.4 移除 layout.ejs 中重复的移动菜单初始化
- `public/js/main.js:56-73` — 已有 `initMobileMenu`
- `views/frontend/layout.ejs:326-354` — 重复的内联版本

**操作**：删除 `layout.ejs` 中的内联版本。

---

## Phase 3: 验证

1. `npm run lint` — 确保无语法错误
2. `npm run dev` — 启动服务器，访问各页面确认功能正常
3. 重点测试：
   - 设置保存（基础、SMTP、协议、弹窗）
   - 文章编辑器的媒体选择器和附件上传
   - 图片分享的管理功能
   - 用户注册和权限授予
   - 首页和 `/home` 重定向

---

## 文件清单

### 新建文件
- `server/utils/format.js`
- `server/utils/fs-helpers.js`
- `server/utils/response.js`
- `public/js/utils.js`
- `public/js/media-selector.js`
- `public/js/attachment-upload.js`

### 修改文件（后端）
- `server/utils/settings.js` — 添加 upsertSettings, saveImageShareConfigs, 修复缓存 key
- `server/config/db-helpers.js` — 添加 grantDefaultPermissions
- `server/config/activity.js` — logActivity 内部错误处理
- `server/config/monitor.js` — 改用 format.js
- `server/config/mailer.js` — getSmtpConfig 改用 getSettings
- `server/app.js` — 删除 getCachedSettings, 删除未用 multer 配置
- `server/routes/frontend.js` — /home 重定向, 错误渲染用 response.js
- `server/routes/auth.js` — generateSecureToken 导入, 默认权限用 db-helpers
- `server/routes/image-share.js` — 删除重复 addLog, 用 isAdminRole, 用 saveImageShareConfigs, 删除旧管理路由
- `server/routes/admin/settings-basic.js` — 用 getSettings + upsertSettings
- `server/routes/admin/settings-smtp.js` — 同上
- `server/routes/admin/settings-agreement.js` — 同上
- `server/routes/admin/settings-popup.js` — 同上
- `server/routes/admin/settings.js` — 同上
- `server/routes/admin/image-share.js` — 用 saveImageShareConfigs + isAdminRole
- `server/routes/admin/users.js` — 用 grantDefaultPermissions
- `server/routes/admin/site-stats.js` — 用 format.js
- `server/routes/admin/attachments.js` — 用 format.js
- `server/routes/admin/backup.js` — 用 format.js + fs-helpers.js
- `server/routes/admin/maintenance.js` — 用 format.js + fs-helpers.js + getSettings + 删除重复定义
- `server/routes/admin/system-update.js` — 用 fs-helpers.js
- `server/routes/admin/projects.js` — 用 getAllProjectDefinitions
- `server/routes/admin/reset.js` — 提取统计收集函数
- `server/routes/admin/articles.js` — 用 response.js
- `server/routes/admin/pages.js` — 用 response.js
- `server/routes/admin/novels.js` — 用 response.js
- `server/routes/admin/messages.js` — 用 isAdminRole
- `server/middlewares/security.js` — 用 response.js
- `server/middlewares/auth.js` — 用 response.js

### 修改文件（前端）
- `views/frontend/layout.ejs` — 引入 utils.js, 删除重复代码
- `views/frontend/article-editor.ejs` — 引入 media-selector.js + attachment-upload.js
- `views/admin/article-editor.ejs` — 同上
- `views/admin/page-editor.ejs` — 引入 media-selector.js
- `public/js/admin.js` — 删除重复函数, 用 utils.js
- `public/js/chat.js` — 删除重复函数, 用 utils.js
- `public/js/main.js` — 删除重复函数, 用 utils.js
- 多个 EJS 模板 — 替换 inline escapeHtml/getCsrfToken 为 utils.js 调用

---

## 执行顺序

1. Phase 1.1-1.2: 创建基础工具文件（无依赖）
2. Phase 1.3: 扩展 settings.js（核心依赖）
3. Phase 1.4-1.8: 修复各处重复引用
4. Phase 1.9: 创建 response.js
5. Phase 1.10-1.15: 修复各处具体重复
6. Phase 2.1: 创建前端 utils.js
7. Phase 2.2-2.3: 提取媒体选择器和附件上传
8. Phase 2.4: 清理 layout.ejs
9. Phase 3: 验证
